{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["schema_version", "artifact_type", "receipt_id", "request_ref", "readiness_ref", "mutation_ref", "readback_ref", "target", "operator_ref", "correlation_ref", "idempotency_key", "completed_at", "phase", "outcome", "receipt_digest"],
  "properties": {
    "schema_version": { "const": 1 },
    "artifact_type": { "const": "workspace-inventory-promotion-receipt" },
    "receipt_id": { "$ref": "#/$defs/text" },
    "request_ref": { "$ref": "#/$defs/ref" },
    "readiness_ref": { "$ref": "#/$defs/ref" },
    "mutation_ref": { "$ref": "#/$defs/ref" },
    "readback_ref": { "$ref": "#/$defs/ref" },
    "target": { "$ref": "#/$defs/target" },
    "operator_ref": { "$ref": "#/$defs/text" },
    "correlation_ref": { "$ref": "#/$defs/text" },
    "idempotency_key": { "$ref": "#/$defs/text" },
    "completed_at": { "type": "string", "format": "date-time" },
    "phase": { "enum": ["review-branch", "merged-authority"] },
    "outcome": { "enum": ["prepared", "replayed", "succeeded"] },
    "receipt_digest": { "$ref": "#/$defs/digest" }
  },
  "$defs": {
    "text": { "type": "string", "minLength": 1 },
    "digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
    "ref": { "type": "object", "required": ["id", "digest"], "properties": { "id": { "$ref": "#/$defs/text" }, "digest": { "$ref": "#/$defs/digest" } }, "additionalProperties": false },
    "target": { "type": "object", "required": ["kind", "name", "record_id"], "properties": { "kind": { "enum": ["repo", "product", "component"] }, "name": { "type": "string", "pattern": "^[a-z0-9][a-z0-9._-]*$" }, "record_id": { "type": "string", "pattern": "^(repo|product|component):[a-z0-9][a-z0-9._-]*$" } }, "additionalProperties": false }
  },
  "additionalProperties": false
}
